{% extends 'base.html.twig' %}
 
{% block title %}Новая книга{% endblock %}
 
{% block body %}
    <div class="container">
        <h2 class="text-center mt-5 mb-3">Добавить книгу</h2>
        <div class="card">
            <div class="card-header">
                <a class="btn btn-outline-info float-right" href="{{ path('app_book_index') }}">
                    Показать список книг
                </a>
            </div>
            <div class="card-body">
                {{ include('book/_form.html.twig') }}
            </div>
        </div>
    </div>
{% endblock %}