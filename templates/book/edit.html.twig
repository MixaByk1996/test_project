{#<!DOCTYPE html>#}

{#<title>Редактировать</title>#}

{#{% block body %}#}
{#    <h1>Обновление</h1>#}

{#    {{ include('book/_form.html.twig', {'button_label': ',Обновить'}) }}#}

{#    <a href="{{ path('app_book_index') }}">Назад</a>#}

{#    {{ include('book/_delete_form.html.twig') }}#}
{#{% endblock %}#}
{% extends 'base.html.twig' %}

{% block title %}Редактировать запись{% endblock %}

{% block body %}
    <div class="container">
        <h2 class="text-center mt-5 mb-3">Редактирование данных книги</h2>
        <div class="card">
            <div class="card-header">
                <a class="btn btn-outline-info float-right" href="{{ path('app_book_index') }}">
                    Показать список книг
                </a>
            </div>
            <div class="card-body">
                {% include 'book/_form.html.twig' with {'button_label': 'Обновить', 'button_color': 'btn-outline-success'}  %}
            </div>
        </div>
    </div>
{% endblock %}