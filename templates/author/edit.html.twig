

{% extends 'base.html.twig' %}

{% block title %}Редактировать запись{% endblock %}

{% block body %}
    <div class="container">
        <h2 class="text-center mt-5 mb-3">Редактирование данных автора</h2>
        <div class="card">
            <div class="card-header">
                <a class="btn btn-outline-info float-right" href="{{ path('app_author_index') }}">
                    Показать список авторов
                </a>
            </div>
            <div class="card-body">
                {% include 'author/_form.html.twig' with {'button_label': 'Обновить', 'button_color': 'btn-outline-success'}  %}
            </div>
        </div>
    </div>
{% endblock %}